import FeedbackCard from 'features/panels/zone/FeedbackCard';

export default function SurveyCard() {
  return (
    <div className="invisible  z-20  sm:visible ">
      <FeedbackCard estimationMethod="no" />
    </div>
  );
}
